# Crawler boundary

This directory will contain the crawler contract and the isolated so-novel adapter in Phase 5.

Phase 2 intentionally contains no so-novel source, binary, rule or integration code. The backend must only depend on its own `CrawlerGateway` contract after the catalog and reader flow work with controlled fixture data.
